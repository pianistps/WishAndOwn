
<%= link_to "Sign Up", new_user_path if !session[:user_id]%><br>
<%= link_to "Sign in with Facebook", url: "auth/:provider/callback" if !session[:user_id]%><br>
<%= link_to "Sign In", new_session_path if !session[:user_id]%><br>
<%= link_to "Log Out", session_path, method: 'delete' if session[:user_id]%>
